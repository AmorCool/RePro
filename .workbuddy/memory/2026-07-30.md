# 2026-07-30 工作日志

## RePro 新仓库搭建完成 + Phase 6 核心移植

### 完成事项
1. **Xcode 项目文件创建** (`RePro.xcodeproj/project.pbxproj`)
   - 双 target 配置：RePro (iOS SwiftUI App) + reprovisiond (Daemon ObjC CLI)
   - 关联全部源文件、entitlements、构建阶段
   - Release 模式手动签名（CODE_SIGN_STYLE = Manual, CODE_SIGN_IDENTITY = "-")

2. **Git 初始提交并推送**
   - 57 文件，5269 行代码（初始骨架）
   - 远程：`https://github.com/AmorCool/RePro.git` (main 分支)

3. **Phase 6: 核心签名逻辑移植完成** (2897 行新增)
   - **EEAppleServices** (976行): Apple Portal API 客户端，内置 zlib GZIP，外部凭证注入
   - **EEProvisioning** (1407行): 4 阶段 provisioning 流程，CSR 生成，白名单过滤，文件存储替代 keychain
   - **EESigning** (605行): ldid 签名核心，PKCS12 构建，entitlements 分析/过滤

### 技术决策记录
- EEAppleServices 移除了 AuthKit/RPVAuthentication/NSData+GZIP 三个外部依赖
- EEProvisioning 改用文件存储（`/var/mobile/Library/Preferences/jp.soh.reprovision/`）替代 SAMKeychain
- EESigning 接口从实例方法改为类方法（更符合 daemon 使用模式）
- 所有注释全中文

### 待后续处理
- CI 构建验证（需要 macOS runner）
- ldid 库集成（EESigning 依赖 ldid.hpp）
- CA 证书资源文件（apple-ios.pem, root.pem）需放入 Bundle
- Podfile 执行（CocoaPods 依赖安装）
